#include <stdio.h>
int main ()
{
	int n,k,d,kupeni;
	float m,sum=0,p;
	scanf ("%d",&n);
	scanf ("%f",&m);
	scanf ("%d",&k);
	d=0;
	kupeni=0;
	do
	{
		d=k*n;
		while(kupeni>n) {
		p=m-0.02*m;}
		sum=sum+k*(m*n-p);
		kupeni++;	
	}while(kupeni<n);
	printf ("sum=%.f",sum);

	return 0;
}
