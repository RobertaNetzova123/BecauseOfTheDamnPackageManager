/*bool ascending;
		if(command == "ordered") {
		ascending = true;
	
		if(second_command == "true") {
			ascending = true;
		}
		else if(second_command == "false") {
			ascending = false;
		}

		if(it == list.end()) {
			list.ordered(ascending);
		}
		else {
			it.ordered(ascending);
		}
	} 
		}
		
	}*/

	/*
	bool ascending;
	if(command == "ordered") {
		ascending = true;
	
		if(second_command == "true") {
			ascending = true;
		}
		else if(second_command == "false") {
			ascending = false;
		}

		if(it == list.end()) {
			list.ordered(ascending);
		}
		else {
			it.ordered(ascending);
		}
	} 
	else if(command == "mul") {
		
	}
	else if(command == "add") {

	}
	else if(command == "show") {
		if(it == list.end()) {
			list.show();
		}
		else {
			it.show();
		}
		cout << endl;
	}
	else if(command == "size") {
		if(it == list.end()) {
			cout << list.size() << endl;
		}
		else {
			cout << it.size() << endl;
		}
	}
	else if(command == "averages") {
		double* average = new double[list.size()];
		list.averages(average);
		for(int i = 0; < list.size(); i++) {
			cout << average[i] << " ";
		}
		cout << endl;
	}
	else if(command == "medians") {
		double* mediana = new double[list.size()];
		list.medians(mediana);
		for(int i = 0; < list.size(); i++) {
			cout << mediana[i] << " ";
		}
		cout << endl;
	}
	else if(command == "sums") {
		double* sum = new double[list.size()];
		list.sums(sum);
		for(int i = 0; < list.size(); i++) {
			cout << sum[i] << " ";
		}
		cout << endl;
	}
	else if(command == "sizes") {
		int* sizee = new double[list.size()];
		list.sizes(sizee);
		for(int i = 0; < list.size(); i++) {
			cout << sizee[i] << " ";
		}
		cout << endl;
	}
	else if(command == "begin") {
		if(it == list.end()) {
			it++;
		}
	}
	else if(command == "next") {
		if(it == list.end() || ++it == list.end()) {
			cout << "ERROR: End of iteration" << endl; 
			break;
		}
	}
	else if(command == "at") {
		
	}
	else if(command == "average") {
		cout << it.average() << ednl;
	}
	else if(command == "median") {
		cout << it.median() << endl;
	}
	else if(command == "sum") {
		cout << it.sum() << endl;
	}
	else {
		cout << "ERROR: Unknown operation" << endl;
	}	


	*/
	
